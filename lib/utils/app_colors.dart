import 'package:flutter/material.dart';

class AppColors {
  // 탭별 테마 색상
  static const character = Color(0xFF9B5DE5);   // 내 캐릭터 — 보라
  static const create = Color(0xFF06D6A0);       // 동화 만들기 — 민트
  static const library = Color(0xFF118AB2);      // 기본 동화 — 파랑
  static const favorites = Color(0xFFEF476F);    // 찜 — 핑크

  // 상단 NavBar / 하단 탭 활성
  static const navBar = Color(0xFFFE9EC7);
  static const activeNav = Color(0xFFFE9EC7);

  // 공통
  static const background = Color(0xFFFFF8F0);   // 크림 배경
  static const cardBg = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFF2D2D2D);
  static const textSecondary = Color(0xFF8A8A8A);
  static const divider = Color(0xFFEEEEEE);
}
